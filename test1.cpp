#include "wszystko.h"
#include <iostream>
#include <stdbool.h>

using namespace std;

int main() {
	char znak = 'a';
	if (czy_znak_jest_litera(znak) == true)
		return 0;
	else return 1;
	
}